<div class="container-fluid">
  <div class="row" id="results">
    <div class="col-md-8 col-md-offset-4">
      <div class="row">
        <div class="col-md-4">
          <h2><%= t ".title", category: @lesson.category.name %></h2>
        </div>
        <div class="col-md-4">
          <h2><%= @lesson.correct_number %>/<%= @lesson.results.count %></h2>
        </div>
      </div>
      <div class="row" id="results">
        <%= render @lesson.results %>
      </div>
      </div>
    </div>
</div>